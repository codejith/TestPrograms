#include <iostream>

int main()
{
    int noTestCases, limit;
    std::cin >> noTestCases;
    while(noTestCases--)
    {
        std::cin >> limit;
        while(limit--)
        {
            for (int i = limit; i>0; --i)
        }
    }

    return 0;
}
